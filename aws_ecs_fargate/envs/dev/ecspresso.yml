region: ap-northeast-1  # AWSリージョン
cluster: {{ env `SERVICE_NAME`}}-cluster  # ECSクラスター名
service: {{ env `SERVICE_NAME`}}-service  # ECSサービス名
service_definition: ecs-service-def.json
task_definition: ecs-task-def.json
timeout: "10m0s"
plugins:
  - name: tfstate
    config:
      url: s3://runble1-tfstate/all_in_one/aws_ecs_fargate/terraform.tfstate
